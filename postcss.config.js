module.exports = {
  plugins: [
    require("autoprefixer")
    // require("autoprefixer")({
    //   // browsers: ["> 1%", "last 2 versions"]
    //   browsers: [">0.2%", "not dead", "not ie <= 11", "not op_mini all"]
    // })
  ]
};
